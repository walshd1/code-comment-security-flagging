# Code Comment Security Flagging

Flags potentially insecure or misleading comments in code using AI to understand context and intent.

## Free
```yaml
- uses: walshd1/code-comment-security-flagging@v1
  with:
    gemini_api_key: ${{ secrets.GEMINI_API_KEY }}
```

## Paid (cost + 4.75%)
```yaml
- uses: walshd1/code-comment-security-flagging@v1
  with:
    service_token: ${{ secrets.ACTION_FACTORY_TOKEN }}
```
